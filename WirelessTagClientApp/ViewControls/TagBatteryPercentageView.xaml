<UserControl x:Class="WirelessTagClientApp.ViewControls.TagBatteryPercentageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WirelessTagClientApp.ViewControls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <Grid>
        <!-- Data context is TagViewModel -->
        <GroupBox Padding="6">
            <GroupBox.Header>
                <TextBlock TextTrimming="CharacterEllipsis" FontWeight="Bold" FontSize="14">
                    <TextBlock.Text>
                        <Binding Path="Name" FallbackValue="???" />
                    </TextBlock.Text>
                    <TextBlock.ToolTip>
                        <TextBlock Text="{Binding Path=LastCommunication, StringFormat={}{0:ddd dd-MMM-yyyy HH:mm:ss} }" />
                    </TextBlock.ToolTip>
                </TextBlock>
            </GroupBox.Header>

            <!-- Battery remaining (%) -->
            <StackPanel
                Orientation="Horizontal">

                <TextBlock 
                    Padding="12,0,0,0"
                    FontSize="36"
                    Text="{Binding Path=BatteryRemaining, FallbackValue=10.1, StringFormat={}{0:n0}}"/>
                <TextBlock
                    Padding="6,0,0,6"
                    FontSize="24"
                    VerticalAlignment="Bottom"
                    Foreground="#404040"
                    Text="%" />
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
